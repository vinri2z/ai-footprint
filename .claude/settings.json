{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"$CLAUDE_PROJECT_DIR/scripts/hooks/require-changeset.sh\"; if [ -f \"$f\" ]; then bash \"$f\"; else exit 0; fi"
          }
        ]
      }
    ]
  }
}
